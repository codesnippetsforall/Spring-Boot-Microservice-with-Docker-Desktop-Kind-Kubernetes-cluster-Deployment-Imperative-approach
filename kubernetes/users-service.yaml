apiVersion: v1
kind: Service
metadata:
  name: users-service
  labels:
    app: users-ws
    tier: backend
spec:
  type: LoadBalancer
  selector:
    app: users-ws
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082